node_modules/
.next/
out/
build/

# banco local e o arquivo de teste
*.db
*.db-journal
prisma/dev.db*
prisma/test.db*

# segredo
.env
.env.local

.DS_Store
*.tsbuildinfo
next-env.d.ts

# client do Prisma: gerado por `prisma generate`, reconstruível
src/gerado/
tsconfig.tsbuildinfo

# derivados locais do expx: reconstruíveis, nunca versionados.
# A `.expx/` NÃO é ignorada inteira — `expx-lock.json` e `marketplace/plugins/expx/`
# são versionados (references/03-fundacao.md).
docs/eventos/
.expx/estado.json
.expx/memoria/
